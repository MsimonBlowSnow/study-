数据定义
	var a; 声明
	a=10; 赋值
	单var 设计
	var a=10,
	    b=20,
	    c=30;
数据类型
	原始值(5种):
	Number Boolean String undefined null
	引用值
typeof 返回值 number undefine function object Boolean String
	其中typeof(null)返回的是object
	
没有声明既可以用typeof(a);//不报错;


NaN===NaN//false
